# OmnissiahCore — requirements.docker.txt
# Lean runtime deps only. No torch, no build tools, no ingestion libs.
# Used exclusively inside the Docker container.

# Core RAG
faiss-cpu>=1.7.4
numpy>=1.26.0
onnxruntime>=1.18.0

# BM25
rank-bm25>=0.2.2

# Embeddings (loads from local bge_m3_onnx/ — no HF download needed at runtime)
sentence-transformers>=2.7.0
huggingface_hub>=0.23.0
tokenizers>=0.19.0

# FastAPI backend
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# HTTP (Ollama calls)
requests>=2.31.0
httpx>=0.27.0

# Utilities
tqdm>=4.66.0